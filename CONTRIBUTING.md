# Contributing

## Development Setup

```bash
git clone https://github.com/openbot-coder/llm-proxy.git
cd llm-proxy
pip install -e ".[dev]"
pytest tests/ -v
```

## Commit Messages

Follow Conventional Commits: `feat:`, `fix:`, `docs:`, `refactor:`, `test:`, `chore:`
